
const Personal = (props: any) => {
    return (
        <>
            <span>{props.name}</span>
            <span>{props.goal}</span>
            <hr />
        </>
    )
}

export default Personal

